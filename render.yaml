services:
  - type: web
    name: random-relay
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m http.server $PORT"
    envVars:
      - key: PORT
        value: 10000
